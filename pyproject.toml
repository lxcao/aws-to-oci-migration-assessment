[project]
name = "aws-to-oci-migration-assessment"
version = "0.1.0"
description = "Reusable AWS-to-OCI migration discovery, assessment, and SOW helper scripts."
requires-python = ">=3.10"
dependencies = [
    "openpyxl>=3.1.0",
    "python-docx>=1.1.0",
]

[tool.uv]
package = false
